apiVersion: v1
kind: Secret
metadata:
  name: ai-agent-platform-secret
  namespace: ai-agent-platform
  labels:
    app.kubernetes.io/name: ai-agent-platform
type: Opaque
stringData:
  # -------------------------------------------------------
  # Replace the values below before applying.
  # For production, use a sealed-secrets controller or
  # an external secret manager (AWS SM, Vault, etc.).
  # -------------------------------------------------------
  OPENAI_API_KEY: "sk-your-openai-api-key"
  SECRET_KEY: "replace-with-a-long-random-string"
