apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ai-agent-platform-pvc
  namespace: ai-agent-platform
  labels:
    app.kubernetes.io/name: ai-agent-platform
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
